# Placeholder videos

`sample-1.mp4` and `sample-2.mp4` are free, CC0-licensed stand-in clips
(sourced from MDN's public sample-video collection), used to preview the
"Watch & Buy" carousel layout on the homepage.

Replace them with real product videos before launch — same filenames (or
update the `SAMPLE_CLIPS` list in
`src/components/home/WatchAndBuy.tsx`), portrait orientation (9:16) works
best for the card shape.
